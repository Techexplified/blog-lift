# Learn more about configuring your app at https://shopify.dev/docs/apps/tools/cli/configuration
#
# When production URL changes: keep `application_url` + [auth].redirect_urls aligned with
# `SHOPIFY_APP_URL` on Vercel (no trailing slash). New Shopify app ⇒ update `client_id` too.

client_id = "4303a2e7d559c86b96bbb6e80d51a4aa"
name = "BlogLift"
application_url = "https://seo-optimizer-app.vercel.app"
embedded = true

[webhooks]
api_version = "2026-01"

  [[webhooks.subscriptions]]
  uri = "/webhooks/compliance"
  compliance_topics = [ "customers/data_request", "customers/redact", "shop/redact" ]

  [[webhooks.subscriptions]]
  topics = [ "app/uninstalled" ]
  uri = "/webhooks/app/uninstalled"

  [[webhooks.subscriptions]]
  topics = [ "app/scopes_update" ]
  uri = "/webhooks/app/scopes_update"

[access_scopes]
# Learn more at https://shopify.dev/docs/apps/tools/cli/configuration#access_scopes
scopes = "read_content,read_products,write_content,write_files,write_products"

[auth]
redirect_urls = [
  "https://seo-optimizer-app.vercel.app/api/auth",
  "https://seo-optimizer-app.vercel.app/api/auth/callback",
  "https://seo-optimizer-app.vercel.app/auth/callback"
]

[build]
automatically_update_urls_on_dev = true
